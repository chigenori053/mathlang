# Pro Demo CLI

Scenarios describing typical research workflows live under `pro/demo/scenarios/`.

- `causal_basic` — runs `pro/examples/polynomial_analysis.mlang` in symbolic mode.

Run:

```bash
python -m pro.cli --file pro/examples/polynomial_analysis.mlang
```

Future work: add dedicated runner mirroring edu demo once additional scenarios are authored.
